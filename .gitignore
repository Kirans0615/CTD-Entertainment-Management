# Dependencies
node_modules/
app/node_modules/

# Build intermediary (not committed — deploy copies to root instead)
dist/
app/dist/

# Local env / system
.DS_Store
*.local
.env
.env.*
.vite/
app/.vite/

# Stale root-level source files (source now lives in app/)
/src/
/vite.config.js
/tailwind.config.js
/postcss.config.js
/package-lock.json

# Built output at root IS committed for GitHub Pages — do NOT ignore:
# index.html, assets/, .nojekyll, 404.html
